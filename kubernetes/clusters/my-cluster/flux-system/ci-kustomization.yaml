apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ci
  namespace: flux-system
spec:
  interval: 1m
  targetNamespace: ci
  path: ./kubernetes/clusters/my-cluster/kubernetes-native/ci
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age