apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: printer
  namespace: flux-system
spec:
  interval: 1m
  targetNamespace: printer
  path: ./kubernetes/clusters/my-cluster/apps/printer
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age