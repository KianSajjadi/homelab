apiVersion: v1
kind: Service
metadata:
  name: {{ include "abiotic-factor.fullname" . }}
  labels:
    {{- include "abiotic-factor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "abiotic-factor.selectorLabels" . | nindent 4 }}
