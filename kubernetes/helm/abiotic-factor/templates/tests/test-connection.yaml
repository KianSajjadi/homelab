apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "abiotic-factor.fullname" . }}-test-connection"
  labels:
    {{- include "abiotic-factor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "abiotic-factor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
